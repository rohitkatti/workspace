syntax = "proto3";

package shared.v1;

// Health service definition for checking the health of the service.
service Health {
  // Modules call this to check if the service is healthy
  rpc Check(HealthCheckRequest) returns (HealthCheckResponse);
}

message HealthCheckRequest {}

message HealthCheckResponse {
  bool healthy = 1;
}