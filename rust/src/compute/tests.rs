// use crate::compute::delaunay::{Segment, Vertex};

// #[test]
// fn test_radius_of_segment() {
//     let v1: Vertex = Vertex { x: 3.0, y: 4.0 };
//     let v2: Vertex = Vertex { x: -7.0, y: 4.0 };

//     let s1: Segment = Segment { st: v1, en: v2 };

//     assert_eq!(s1.length(), 10.0);
// }
